<?xml version="1.0"?>
<ruleset name="Rainmaker PSR-2">
    <description>PHP CodeSniffer ruleset for Rainmaker</description>

    <!-- Include main PSR-2 rules -->
    <rule ref="PSR2" />

    <exclude-pattern>tests/*</exclude-pattern>
    <exclude-pattern>vendor/*</exclude-pattern>
</ruleset>
